spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_code_agent_test
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379

app:
  user:
    provider: local
    session-ttl: 30m
